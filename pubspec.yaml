name: gudang_pro
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.1.2
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  csv: ^6.0.0
  path_provider: ^2.1.3
  open_file: ^3.3.2
  shimmer: ^3.0.0
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^9.2.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2 # Versi ini sudah disesuaikan

# Bagian ini adalah opsional, bisa ditambahkan jika Anda ingin mengganti ikon aplikasi
# flutter_launcher_icons: ^0.13.1

# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: true
#   image_path: "assets/images/logo.png"
#   min_sdk_android: 21


flutter:
  uses-material-design: true

  assets:
    - assets/images/